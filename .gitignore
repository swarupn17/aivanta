# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# ---- Node / Next.js ----
node_modules/
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# ---- env / secrets ----
.env
.env*.local
.env.local

# ---- python (legacy tooling) ----
venv/
__pycache__/

# ---- vercel ----
.vercel

# Never commit PII / student data files
*student*.csv
*students*.xlsx
*pii*

# Source / reference & internal docs - NOT for the public repo or live site
# (brand source PNGs, brochure PDF, business-strategy notes, PRDs, raw syllabus dump)
temp/
docs/

# NOTE: /drizzle migrations ARE committed on purpose (schema history).
