# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Dependencies / build (for future backend phase)
node_modules/
venv/
.env
__pycache__/

# Never commit PII / student data files
*student*.csv
*students*.xlsx
*pii*

# Source / reference & internal docs - NOT for the public repo or live site
# (brand source PNGs, brochure PDF, business-strategy notes, PRDs, raw syllabus dump)
docs/
